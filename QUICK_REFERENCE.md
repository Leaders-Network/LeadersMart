# 📋 Quick Reference Card

## 🚀 Start Development
```bash
npm run dev
```

## 🔐 Environment Variables

Your `.env.local` (already configured):
```env
MONGODB_URI=mongodb://localhost:27017/shophub
JWT_SECRET=<auto-generated-secure-key>
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

## 📍 Important URLs

| URL | Purpose |
|-----|---------|
| http://localhost:3000 | Home page |
| http://localhost:3000/signup | Create account |
| http://localhost:3000/login | Login |
| http://localhost:3000/dashboard | User dashboard (protected) |
| http://localhost:3000/cart | Shopping cart (protected) |
| http://localhost:3000/test-cart | Test cart features (protected) |

## 🗄️ MongoDB Collections

| Collection | Purpose |
|------------|---------|
| `users` | User accounts (name, email, hashed password) |
| `carts` | User shopping carts (items, quantities) |

## 🔑 Authentication Flow

```
Signup → Hash password → Save to MongoDB → Auto-login → JWT token → Dashboard
Login → Verify password → JWT token → Dashboard
Protected page → Check JWT → Allow/Deny access
30 minutes → Auto-logout → Clear session → Redirect home
```

## 📦 Key Features

| Feature | Status | Location |
|---------|--------|----------|
| MongoDB Auth | ✅ | `/api/auth/*` |
| Cart Sync | ✅ | `/api/cart` |
| Success Toasts | ✅ | Global |
| Auto Logout | ✅ | 30 minutes |
| Cart Preview | ✅ | Header hover |
| Protected Routes | ✅ | AuthGuard |

## 🛠️ Common Commands

```bash
# Start development
npm run dev

# Build for production
npm run build

# Start production server
npm start

# Check MongoDB connection
# Look for: ✅ MongoDB connected successfully
```

## 🔧 Troubleshooting

| Issue | Solution |
|-------|----------|
| MongoDB connection failed | Check if MongoDB is running |
| JWT error | Clear localStorage, login again |
| Cart not syncing | Make sure you're logged in |
| Auto-logout not working | Check browser console |

## 📚 Documentation

| File | What's Inside |
|------|---------------|
| **FINAL_SETUP_SUMMARY.md** | Complete overview |
| **ENV_SETUP.md** | Environment variables |
| **AUTHENTICATION_GUIDE.md** | How auth works |
| **QUICKSTART.md** | 5-minute setup |
| **MONGODB_SETUP.md** | MongoDB configuration |

## 🎯 Test Checklist

```
□ MongoDB connected
□ Created account
□ Logged in successfully
□ Added items to cart
□ Saw success toast
□ Cart badge updated
□ Cart preview works
□ Cart saved to MongoDB
□ Auto-logout works
```

## 💡 Quick Tips

1. **Check MongoDB**: Look for "✅ MongoDB connected successfully" in console
2. **Test Auth**: Use `/signup` and `/login` pages
3. **Test Cart**: Use `/test-cart` page
4. **Check Data**: Use MongoDB Compass or Atlas dashboard
5. **Debug**: Check browser console and terminal for errors

## 🔐 Security Notes

- ✅ Passwords hashed with bcrypt (12 rounds)
- ✅ JWT tokens expire after 30 minutes
- ✅ HTTP-only cookies in production
- ✅ Auto-logout on token expiry
- ✅ `.env.local` in `.gitignore`

## 🚀 What We're Using

- Next.js 16 + React 19
- MongoDB + Mongoose
- JWT authentication
- bcrypt password hashing
- Tailwind CSS
- TypeScript

## ❌ What We're NOT Using

- NextAuth (custom JWT instead)
- Refresh tokens (30-min sessions)
- OAuth providers (email/password only)
- External auth services

---

**Everything you need on one page! 📄**

For detailed info, check the other documentation files.
